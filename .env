# Backend server URL — used by Vite to configure the frontend API endpoint.
# Set this to your deployed Render backend URL so the live Firebase site can
# reach the backend server. For local development, this is ignored (falls back to localhost:5000).
VITE_SERVER_URL=https://abc-rally.onrender.com
